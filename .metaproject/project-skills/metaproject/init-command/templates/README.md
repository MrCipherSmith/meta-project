# Templates

Version: 0.1.0

This directory stores reusable templates for project skill `metaproject/init-command`.

Add templates only when a repeated implementation pattern becomes stable enough to reuse.
