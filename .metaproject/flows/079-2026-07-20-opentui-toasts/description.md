# Flow 079 — toast messages

A sidebar-bottom transient toast: `✓ Copied to clipboard` on copy-on-select, auto-dismiss after 5s or on the next toast. Plus a transparency note in the OpenRouter key prompt (session-only, not saved to disk). /model + /connection + persistence are flow 080.
