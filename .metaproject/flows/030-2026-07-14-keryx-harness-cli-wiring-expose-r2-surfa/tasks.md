# Tasks — Flow 030 (keryx harness CLI wiring)

Statuses via `keryx flow task done 030 <taskId>`. TDD. Reuse-only; deterministic/offline; deps `{}`;
fail-closed; D-02 (commands never write flow.json); no real spawn in CI; frozen untouched. Root
`/Users/Goodea/goodea/keryx` (branch feature/keryx-harness-cli-wiring). Worktree-guard. Commits/PR:
NO co-authorship. Mirror `src/commands/harness.ts`'s `run` pattern (injectable deps, fail-closed
guards, structured JSON, never persists flow state).

| ID | Kind | Model | Title / definition |
|----|------|-------|--------------------|
| T1 | context | Haiku | Scope + harness.ts reuse pattern + R2 input shapes (context.md). |
| T2 | implement | — | Umbrella: implement per plan (closed when T6 done). |
| T3 | test | — | Umbrella: TDD tests (closed when T5 authored + green). |
| T4 | review | — | Umbrella: review + completion (closed when T7 + completion done). |
| T5 | test (RED) | Sonnet | RED CLI-command tests (OFFLINE; injected deps: clock/idSeq/env + a fake `ProcessAdapter` + an injected spec; no real spawn/fs). **exec**: (a) NO `--allow-real-subprocess`/`KERYX_ALLOW_REAL_SUBPROCESS` → the command prints a refusal and returns, adapter NEVER constructed (assert no spawn); (b) with an injected `FakeProcessAdapter` (deps) + approved argv/env → prints ONE JSON blob `{outcome:{kind:"completed",exitCode?},receipt,evidenceRefs}`; (c) fake timeout/output-overflow/cancel → the matching non-success outcome (never `completed`); (d) unapproved argv/env or budget breach → `{outcome:{kind:"blocked",reason}}`, spawn spy = 0; (e) the last stdout line is valid JSON and NO flow.json is written. **extension**: (f) valid injected spec (manifest+grant+task) → `{registration:{ok:true},dispatch,result,evidenceRefs}` (canonical subagent-dispatch/result); (g) invalid/denied registration → fail-closed error result, NO dispatch; (h) an escalating grant lacking policy∧provenance∧approval → denied. **wave**: (i) valid spec (registered tasks + `{maxConcurrency,parentRemaining}`) → `{ok:true,waves}` with no wave > maxConcurrency + aggregate budget respected; (j) unregistered task / cycle / budget breach → `{ok:false,reason}`. **smoke**: (k) flag-gated (`KERYX_ALLOW_REAL_SUBPROCESS=1`) CI-inert real-exec of `/bin/echo` via `describe.skipIf`, 0 spawns without the flag. RED before T6. |
| T6 | impl | Opus | GREEN. Add `exec`/`extension`/`wave` subcommands (route `args[0]` in `harnessCommand` alongside `run`; extract sibling helpers/modules if cleaner — e.g. `src/commands/harness-exec.ts` etc.) + route in `src/cli.ts` + extend the usage/`--help`. **exec**: parse `[--allow-real-subprocess] [--cwd <dir>] [--timeout-ms <n>] [--max-output-bytes <n>] [--allow-env K1,K2] -- <program> <args…>`; fail-closed without the flag (refuse, no adapter); build `ContainedCommand`/allowlist(GuardInput)/budget/`parentRemaining`/`outputLimitBytes`; adapter = injected `deps.processAdapter` ?? (flag ? new `RealProcessAdapter` : refuse); call REUSED `runContainedProcess`; print `{outcome,receipt?,evidenceRefs}`. **extension**: read the spec from `deps.spec` ?? `--spec <file>` (fs read only for the real path); REUSED `registerExtension` (fail-closed on deny) → build `DispatchExtensionInput` → REUSED `dispatchExtension`; print `{registration,dispatch,result,evidenceRefs}`. **wave**: spec → `ExtensionWaveTask[]`+config → REUSED `planExtensionWave`; print `{ok,waves|reason}`. Injectable deps (adapter/spec/clock/idSeq/env) mirroring harness.ts. Fail-closed guards FIRST. NEVER write flow.json (D-02). Secrets (env values/API key) never printed. Reuse-only; deps `{}`; no real spawn in the offline path. Make T5 green. |
| T7 | review | Opus | code-verifier (`tsc` + full `bun test` ≥1338/2skip + new green); **exec fail-closed** (no spawn without the flag — adversarial; unapproved argv/env/budget → blocked; RealProcessAdapter gated + never constructed in the offline path); **extension/wave fail-closed** (unregistered/denied registration → no dispatch; escalation requires policy∧provenance∧approval; unregistered task/cycle/budget → wave denied); **D-02** (`ctx rg writeFlow|flow.json src/commands` — the new commands write NO flow state); **secrets** (env VALUES / API key never in output/logs — only allowlisted KEYS + typed outcomes); reuse-only (`runContainedProcess`/`dispatchExtension`/`planExtensionWave`/`registerExtension`/harness.ts run path unmodified or additive — `git diff`); determinism (offline tests inject adapter/spec; no real spawn in CI; no Date.now/Math.random in the command core beyond injectable clock/id); deps `{}` (child_process stdlib); no regression; frozen requirements pkg + canonical schemas + src/eval + src/contracts + ADR untouched. Lens: security. |
