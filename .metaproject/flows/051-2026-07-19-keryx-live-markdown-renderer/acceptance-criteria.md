# Acceptance Criteria — flow 051 (agent live-markdown differential renderer)

- AC1: A new PURE module `src/lib/live-render.ts` provides `stripAnsi`, `displayWidth` (ANSI-stripped, wide-char aware, best-effort), `physicalRows(lines, cols)`, and `computeRepaint({prevRows, nextLines, cols, sync})→{output, rows}`. `computeRepaint` emits the minimal control string to transform a previously-painted block into the next one: cursor-up over the previous physical rows, `ESC[0J` clear-to-end, then the new lines; when `sync` is set the whole repaint is wrapped in synchronized-output markers (`ESC[?2026h`/`ESC[?2026l`). No IO/clock/randomness. First paint (`prevRows:0`) emits no cursor-up.
- AC2: A stateful `LiveMarkdownBlock` (injectable `out`/`cols`/`render`/`sync`) accumulates streamed text via `append`, repaints on `flush` (only when dirty), and on `finalize` does a final repaint + newline + state reset. A mid-stream terminal-width change (cols differs from the cols used for the outstanding block) degrades safely to a fresh block (newline break, no bogus cursor-up) — NO SIGWINCH handler is registered (flow-048 readline constraint honored). Fully unit-tested via injected `out`.
- AC3: `runAgentRepl` uses `LiveMarkdownBlock` for live, per-token markdown streaming ONLY when `colorEnabled() && process.stdout.isTTY`; otherwise it keeps the exact flow-050 behavior (no live paint; `onAssistantText` renders once). Repaints are coalesced on a timer (≤ ~20/s) so a fast/long stream is not O(n²) per token. The driver (`runAgentTurn`) and its hooks are unchanged; chat mode and `roleLabel` are untouched.
- AC4: `bunx tsc --noEmit` clean; `bun test` green with no reduction from baseline (1460 pass); new unit tests cover `stripAnsi`/`displayWidth`/`physicalRows`/`computeRepaint` (first paint, repaint, sync wrap, wrap-aware row count) and `LiveMarkdownBlock` (append→flush→finalize sequence, dirty-skip, width-change fresh-block). No new runtime dependency.
