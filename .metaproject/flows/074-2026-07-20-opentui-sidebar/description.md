# Flow 074 — sidebar + reasoning expand + timestamps + tokens

Toward opencode/grok: a right status sidebar (Model/Context/Tools), an expandable reasoning (/think), message timestamps, and an always-visible token counter (header + sidebar).
