# Tasks — Flow 022 (Release 2 · R2-4 interactive CLI/TUI)

Task definitions live here; statuses via `keryx flow task done <id> <taskId>`.

Scope: R2-4 (E-03 §4 AC-R2-4 · SC_R13_TUI). Provider/model detect + picker (no hardcoded
default) + slash commands + output polish over the flow-021 shell. New `src/commands/
select.ts`; additive `shell.ts` + `cli.ts`. Variant A — NO new dep (readline+ANSI). Reuse
flow-021 core + W14/W20 providers + W15/W20 egress unchanged. Tests OFFLINE (injected IO/
fetch/env + FakeProvider); live smoke manual. `deps {}`. Credential never stored. D-02.
Root `/Users/Goodea/goodea/keryx` (branch feature/keryx-release2-tui). Worktree-guard.
Commits/PR: NO co-authorship.

| ID | Kind | Model | Title / definition |
|----|------|-------|--------------------|
| T1 | context | Haiku | R2-4 scope + reuse surface + testable-core contract (context.md). |
| T2 | implement | — | Umbrella: implement per plan (closed when T6 done). |
| T3 | test | — | Umbrella: TDD tests (closed when T5 authored + green). |
| T4 | review | — | Umbrella: review + live smoke + completion (closed when T7/T8 + completion done). |
| T5 | test (RED) | Sonnet | OFFLINE RED tests. **(a)** `src/commands/select.test.ts` — `detectProviders({fetch, env, baseUrl?})`: an injected `fetch` returning a recorded ollama `/api/tags` body lists its CHAT models (embedding families e.g. `nomic-bert`/`*-embed` excluded); an injected `fetch` that throws / non-200 → ollama "not available" (fail-soft, no throw); `env.ANTHROPIC_API_KEY` set → anthropic available with a static known-model list, unset → not available; `fake` always available. `pickProviderModel(io, detected)`: an `io.lines` async generator of numbered choices → returns the correct `{provider, model, baseUrl?}`; an out-of-range/non-numeric choice re-prompts or falls back safely (assert no crash + a deterministic result); menus are written to `io.write`. Deterministic (no Date.now/Math.random). **(b)** `src/commands/shell.test.ts` additions — `/models` lists the current provider's models and a numeric pick switches the model for the next turn; `/provider` re-runs selection; `/connect` writes guidance mentioning `ANTHROPIC_API_KEY` and does NOT store/echo a key; existing flow-021 shell tests stay green. RED before T6. |
| T6 | impl | Opus | GREEN. `src/commands/select.ts`: `export async function detectProviders(deps): Promise<DetectedProvider[]>` (`DetectedProvider = { name: "ollama"|"anthropic"|"fake"; models: string[]; baseUrl?: string }`) + `export async function pickProviderModel(io: ShellIO, detected: DetectedProvider[]): Promise<{ provider: string; model: string; baseUrl?: string }>` (numbered readline picker; no hardcoded default). Additive `src/commands/shell.ts`: add `/models`/`/provider`/`/connect` handling in `runShell` (reuse the existing loop; `/provider` and `/models` may call an injected selector so they stay offline-testable); replace `shellCommand`'s hardcoded `provider="ollama"`/`model="llama3.1:latest"` — when `--provider` is absent, run `detectProviders` (real fetch/env) + `pickProviderModel` (real stdin) BEFORE `runShell`; when `--provider X [--model Y]` given, skip the picker; output polish (a `\n`/separator after each turn, a `> ` indicator, provider/model in the banner/prompt). Minimal `cli.ts` help note. Ollama detection probes `{baseUrl}/api/tags` (default loopback) reusing the W20 grant posture. Credential (`ANTHROPIC_API_KEY`) only READ from env; `/connect` prints how-to, never stores. `node:readline` stdlib, NO new dep. Guard array indexing. Make T5 green. |
| T7 | review | Opus | code-verifier (`tsc` + full `bun test` ≥1210 + new green, ALL OFFLINE — no real TTY/network in tests); no hardcoded provider default remains (bare shell runs detect+pick); `detectProviders` fail-soft on unreachable ollama (no crash) while chat egress stays loopback-gated (W20/W15 unchanged); `pickProviderModel` handles invalid input without crash; credential never stored/entered/logged (`ANTHROPIC_API_KEY` env-read only; `/connect` guidance only); reuse-only (flow-021 `runShell` core + W14/W20 providers + W15/W20 egress UNMODIFIED; only additive edits to shell.ts + cli.ts + new select.ts); `deps {}` (node:readline stdlib, no framework/SDK); D-02 (no flow.json write); no runtime-contract change (SC_R13_TUI — adapter over existing ports); no regression (flow-021 shell tests green); frozen requirements pkg + canonical schemas + src/eval + src/contracts + ADRs untouched. Lens: security + UX + contract. |
| T8 | verify (live smoke) | orchestrator | Live: bare `keryx` against the running Ollama → detection lists providers → pick provider+model → multi-turn chat streams; `/models` lists real ollama models + switch; `keryx --provider anthropic` with no key → clear fail-closed message (no network). Also `keryx --provider ollama --model llama3.1:latest "..."`-style explicit path still works. Manual; NOT CI. Record in the flow journal. |
