# Tasks — Flow 026 (Release 2 · R2-5 real-subprocess executor)

Task definitions live here; statuses via `keryx flow task done 026 <taskId>`.

Scope: R2-5 (E-03 §4 AC-R2-5) — SC_R04_SHELL_CONTAINMENT runtime half / F-1. New
`src/harness/process/`; additive-only. Reuse W10 guardAction/actionFingerprint/ExecutionReceipt
+ W12 inheritBudget + W7 evidence + W8 receipt. Offline suite = fake adapter ONLY; thin real
`node:child_process` adapter behind `allowRealSubprocess`, live smoke NOT in CI. NO new dep
(child_process is stdlib; `deps {}`). Deterministic (injected id/clock). Fail-closed. D-02. Root
`/Users/Goodea/goodea/keryx` (branch feature/keryx-release2-subprocess-executor). Worktree-guard.
Commits/PR: NO co-authorship.

| ID | Kind | Model | Title / definition |
|----|------|-------|--------------------|
| T1 | context | Haiku | R2-5 scope + SC_R04_SHELL_CONTAINMENT runtime half + F-1 + reuse surface (context.md). |
| T2 | implement | — | Umbrella: implement per plan (closed when T6 done). |
| T3 | test | — | Umbrella: TDD tests (closed when T5 authored + green). |
| T4 | review | — | Umbrella: review + completion (closed when T7 + completion done). |
| T5 | test (RED) | Sonnet | `src/harness/process/executor.test.ts` (OFFLINE, injected id/clock + a scripted fake `ProcessAdapter` with a spawn spy/counter) + a flag-gated CI-skipped `real-process-adapter.smoke.test.ts`. Cases (each fail-closed): **(a) happy path** — approved argv+env, clean in-bounds exit → `{kind:"completed"}` + schema-valid `ExecutionReceipt` + non-empty evidenceRefs; ran in the approved cwd. **(b) timeout** — scripted observation exceeds the reserved deadline → `{kind:"timeout"}`, group killed, NOT completed/success, receipt records timeout. **(c) output-overflow** — output > `outputLimitBytes` → `{kind:"output-overflow"}`, bounded, NOT success, no unbounded-retry signal. **(d) cancellation** — external `cancelled` → `{kind:"cancelled"}`, group killed, NOT success. **(e) unapproved argv/env (fail-closed)** — argv not allowlisted / non-allowlisted env / injection → `{kind:"blocked"}`, and the fake adapter's `spawn` was **NEVER called** (assert spy count = 0). **(f) budget breach** — reserved runtime > parent remaining → blocked (reuse `inheritBudget`); adapter never spawned. **(g) spawn-error / ambiguous** — non-success (blocked), never a false `completed`. **(h) no-orphan** — the kill path targets the whole GROUP (fake records a group-kill, not just the leader). **(i) determinism** — same input+deps twice → deep-equal outcome. **(j) live smoke** — only when `allowRealSubprocess` + env flag set: a trivial real `node:child_process` runs and a real timeout kills the group; skipped by default (assert it does NOT run in CI). RED before T6. |
| T6 | impl | Opus | GREEN. `src/harness/process/executor.ts`: `ProcessAdapter` port (`spawn(command): ProcessObservation`), `ContainedCommand` (reuse/extend `ActionSpec` + cwd), `ContainedProcessOutcome` (`completed`{receipt;evidenceRefs} \| `timeout`\|`output-overflow`\|`cancelled`{receipt} \| `blocked`{reason}), `runContainedProcess(input, deps: {idSeq; clock})`: (1) allowlist via REUSED `actionFingerprint`+`guardAction` — deny → blocked, adapter NEVER spawned; (2) budget via REUSED `inheritBudget` — breach → blocked; (3) spawn once via `input.adapter` in the approved cwd; (4) classify the observation → typed outcome; build a schema-valid `ExecutionReceipt` (REUSE W8/W10 shape) + a W7 `EvidenceRecord`; every non-`completed` observation → a recorded NON-success. `fake-process-adapter.ts` — deterministic scripted adapter (no real spawn; records group-kills). `real-process-adapter.ts` — thin `node:child_process` (`spawn({detached:true, cwd})` + `process.kill(-pid)`), ONLY constructed behind `allowRealSubprocess`, never in CI. Injected id/clock; guard indexing; `exactOptionalPropertyTypes` safe; NO real spawn/fs/network in the offline path; writes no flow.json; secrets never logged. NO new dep. Make T5 green. |
| T7 | review | Opus | code-verifier (`tsc` + full `bun test` ≥1254 + new green, live smoke skipped); **allowlist fail-closed** (unapproved argv/env NEVER spawns the adapter — the security core; adversarial: any bypass?); **bound-hit non-success** (timeout/overflow/cancel can NEVER report `completed` — adversarial); **budget breach fail-closed**; **no-orphan** (group kill, not leader-only); **real adapter gating** (unreachable without `allowRealSubprocess`; the offline suite performs NO real spawn — grep the suite/CI); **D-02** (`ctx rg writeFlow|flow.json src/harness/process` = 0; parent owns completion via ManagedFlowPort); reuse-only (W10 guard/fingerprint/execute, W8 receipt, W12 inheritBudget, W7 evidence unmodified or additive — `git diff` confirms); determinism (no Date.now/Math.random; fake adapter pure; no real spawn in CI); deps `{}` (child_process stdlib, no framework); secrets (env values) never logged/persisted; frozen requirements pkg + canonical schemas + src/eval + src/contracts + ADR-0001..0004 untouched; no regression. Lens: highload + security. |
