# Tasks — Flow 024 (Release 2 · R2-2 registered-extension provenance)

Task definitions live here; statuses via `keryx flow task done 024 <taskId>`.

Scope: R2-2 (E-03 §4 AC-R2-2) — 2 scenarios (SC_R18_REGISTERED_EXTENSION_PROVENANCE,
SC_R18_EXTENSION_ESCALATION_REQUIRES_POLICY registry-side). New `src/harness/extension/
provenance.ts`; additive-only. Reuse W15 registry + R2-1 execute.ts (evaluateExtensionGrant)
+ W12 childProvenance + W7 Provenance + W10 approval. NO new dep/SDK/network (`deps {}`).
Deterministic (injected id/clock). Fail-closed (authority not widened; out-of-grant → deny/
ask). D-02. Root `/Users/Goodea/goodea/keryx` (branch feature/keryx-release2-ext-provenance).
Worktree-guard. Commits/PR: NO co-authorship.

| ID | Kind | Model | Title / definition |
|----|------|-------|--------------------|
| T1 | context | Haiku | R2-2 scope + 2 scenarios + reuse surface + integration map (context.md). |
| T2 | implement | — | Umbrella: implement per plan (closed when T6 done). |
| T3 | test | — | Umbrella: TDD tests (closed when T5 authored + green). |
| T4 | review | — | Umbrella: review + completion (closed when T7 + completion done). |
| T5 | test (RED) | Sonnet | `src/harness/extension/provenance.test.ts` (OFFLINE, injected id/clock). **(a) registerExtensionWithProvenance (SC_R18_REGISTERED_EXTENSION_PROVENANCE):** a `RegisterExtensionInput` with a pinned `manifest` (manifestHash) + a non-empty `capabilityGrant` → `registerExtension` ok → the returned `ExtensionProvenanceRecord` has `extensionId`, `manifestHash` === the pinned manifest hash, `grantId` === the grant's, `capabilities` === EXACTLY the grant's `capabilities` (deep-equal; assert NO capability beyond the grant — authority not widened), and a `provenance: Provenance` with `trustLevel:"derived"` + a `provenanceId` from `deps.idSeq()` + taint linkage. A missing-manifest / empty-grant input → `registerExtension` denies → `registerExtensionWithProvenance` propagates `{ok:false}` and produces NO provenance record. Deterministic (identical inputs+deps twice → deep-equal). **(b) evaluateRegisteredExtensionCapability (SC_R18_EXTENSION_ESCALATION_REQUIRES_POLICY, NEGATIVE):** a requested capability IN the grant → `{ok:true}`; a capability OUTSIDE the grant WITHOUT policy → DENIED; without provenance → DENIED; without a valid approval → DENIED; with policy+provenance+valid-approval → ok (approval path); a `policyDecision:"ask"` (or missing approval) surfaces an ask-for-approval outcome (assert the "denied OR asks" per the scenario); out-of-enum capability → fail-closed. Reuse R2-1 `evaluateExtensionGrant`. RED before T6. |
| T6 | impl | Opus | GREEN. `src/harness/extension/provenance.ts`: `ExtensionProvenanceRecord` type; `registerExtensionWithProvenance(input, deps: {idSeq; clock; registrationProvenance?: Provenance}): {ok:true; record: ExtensionProvenanceRecord}|{ok:false; reason}` (compose W15 `registerExtension`; on ok build the record with pinned manifestHash + grantId + capabilities copied from the grant (never widened) + a derived `Provenance` via `childProvenance`/a fresh derived provenance; the record is the returned "persisted" form — no fs); `evaluateRegisteredExtensionCapability(input, deps: {checkApproval})` (in-grant ok; out-of-grant → reuse `evaluateExtensionGrant` deny/ask; fail-closed out-of-enum). Additive registry helper ONLY if strictly needed. Injected id/clock; guard array indexing; `exactOptionalPropertyTypes` safe. NO new dep; writes no flow.json. Make T5 green. |
| T7 | review | Opus | code-verifier (`tsc` + full `bun test` ≥1254 + new green); **authority-not-widened (KEY):** registration NEVER yields a capability beyond the grant (adversarial — can a record carry an extra capability?); **provenance integrity:** pinned manifestHash + grantId + granted capabilities + derived-trust `Provenance` persisted; **escalation fail-closed (KEY):** an out-of-grant capability → deny/ask, never a silent grant (adversarial — reuse of `evaluateExtensionGrant`); **D-02** (`ctx rg writeFlow|flow.json src/harness/extension/provenance.ts` = 0); reuse-only (W15/R2-1 execute.ts/W12/W7/W10 unmodified or additive-only — `git diff` confirms); determinism (no Date.now/Math.random); deps `{}` (no SDK/network); frozen requirements pkg + canonical schemas + src/eval + src/contracts + ADR-0001..0004 untouched; no regression. Lens: security + contract. |
