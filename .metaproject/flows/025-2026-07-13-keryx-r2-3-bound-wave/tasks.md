# Tasks — Flow 025 (Release 2 · R2-3 bound-parallel-wave over registered extensions)

Task definitions live here; statuses via `keryx flow task done 025 <taskId>`.

Scope: R2-3 (E-03 §4 AC-R2-3) — SC_R08_BOUND_PARALLEL_WAVE. New `src/harness/extension/
bound-wave.ts`; additive-only. Reuse W13 planWaves + R2-1 dispatchExtension + W12 inheritBudget
+ W15 registry + W7 evidence + W8 immutable. NO new dep/SDK/network/real-async (`deps {}`).
Deterministic (injected id/clock). Fail-closed. D-02. Root `/Users/Goodea/goodea/keryx` (branch
feature/keryx-release2-bound-wave). Worktree-guard. Commits/PR: NO co-authorship.

| ID | Kind | Model | Title / definition |
|----|------|-------|--------------------|
| T1 | context | Haiku | R2-3 scope + SC_R08_BOUND_PARALLEL_WAVE + reuse surface + integration map (context.md). |
| T2 | implement | — | Umbrella: implement per plan (closed when T6 done). |
| T3 | test | — | Umbrella: TDD tests (closed when T5 authored + green). |
| T4 | review | — | Umbrella: review + completion (closed when T7 + completion done). |
| T5 | test (RED) | Sonnet | `src/harness/extension/bound-wave.test.ts` (OFFLINE, injected id/clock + fake adapters). Mirror the scenario: build 3 REGISTERED-extension wave tasks (each `registration = registerExtension(ok)` + a `capabilityGrant` + a `budgetRequest`), all independent (no deps), a config `{maxConcurrency:2, parentRemaining: <enough>}`. **(a) concurrency ceiling:** `planExtensionWave(...)` → `{ok:true, waves}` where NO wave's `taskIds.length > 2` (3 ready + max 2 → e.g. [2,1]). **(b) aggregate budget:** a set whose Σ budgetRequests ≤ parentRemaining → ok with Σ granted ≤ parent; a set whose Σ would breach → `{ok:false}` (reason mentions budget — propagated from planWaves). **(c) registered-only:** a task whose `registration.ok===false` → `{ok:false}` (reason mentions registration; no wave binds to an unregistered extension). **(d) per-task extension dispatch:** each scheduled task carries a canonical dispatch (via R2-1 dispatchExtension) bounded to its grant (assert `allowed_actions`/dispatch present + bounded). **(e) per-attempt evidence history:** each task/attempt carries its OWN `EvidenceRecord` (assert distinct evidence per task; a second attempt of a task does NOT mutate the first attempt's evidence — `.toThrow()` on frozen prior OR deep-equal after). **(f) cycle / degenerate concurrency** (a dependency cycle, or maxConcurrency<1) → `{ok:false}` (propagated from planWaves). **(g) determinism:** identical input+deps twice → deep-equal plan. RED before T6. |
| T6 | impl | Opus | GREEN. `src/harness/extension/bound-wave.ts`: `ExtensionWaveTask` + `BoundWave` types; `planExtensionWave(input, deps: {idSeq; clock; checkApproval?})`: (1) verify every task's `registration.ok` — else fail-closed deny; (2) map to W13 `ChildTask[]` (taskId/dependsOn/budgetRequest) + call REUSED `planWaves(childTasks, {maxConcurrency, parentRemaining}, {idSeq})` — propagate its deny (budget/cycle/degenerate); (3) for each scheduled task build a canonical dispatch via REUSED R2-1 `dispatchExtension` (bounded to grant); (4) each task/attempt gets its OWN `EvidenceRecord` (reuse `childResultToEvidence`/W7 evidence; attempts immutable — reuse W8; freeze per-attempt records). Return `{ok:true; waves: BoundWave[]}|{ok:false; reason}`. Injected id/clock; guard array indexing; `exactOptionalPropertyTypes` safe; NO real async (pure). NO new dep; writes no flow.json. Additive helper to scheduler/execute ONLY if strictly needed. Make T5 green. |
| T7 | review | Opus | code-verifier (`tsc` + full `bun test` ≥1254 + new green); **concurrency ceiling** (no wave > maxConcurrency — reuse of planWaves intact); **aggregate budget fail-closed** (Σ ≤ parent — adversarial: can Σ granted exceed parent?); **registered-only** (an unregistered extension can NEVER bind to a wave — fail-closed); **per-attempt evidence isolation** (one attempt's evidence never mutates another's; attempts immutable — adversarial); **per-task dispatch bounded** to its grant (reuse R2-1); **D-02** (`ctx rg writeFlow|flow.json src/harness/extension/bound-wave.ts` = 0; no flow.json write; parent owns completion via ManagedFlowPort); reuse-only (W13 planWaves / R2-1 execute.ts / W12 / W7 / W8 unmodified or additive-only — `git diff` confirms); determinism (no Date.now/Math.random; no real async); deps `{}` (no SDK/network); frozen requirements pkg + canonical schemas + src/eval + src/contracts + ADR-0001..0004 untouched; no regression. Lens: highload + security. |
