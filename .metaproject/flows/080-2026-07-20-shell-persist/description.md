# Flow 080 — persist provider/model + key

Like opencode auth.json: persist the last provider/model + OpenRouter key to ~/.local/share/keryx/auth.json (0600), load on startup so the user need not re-enter. /connect + /model switching is flow 081.
