# Tasks — Flow 023 (Release 2 · R2-1 extension-execution)

Task definitions live here; statuses via `keryx flow task done 023 <taskId>`.

Scope: R2-1 (E-03 §4 AC-R2-1) — 3 scenarios (SC_R08_CHILD_DISPATCH_CANONICAL_RESULT,
NEEDS_CONTEXT_ADAPTER, EXTENSION_ESCALATION_REQUIRES_POLICY). New `src/harness/extension/
execute.ts`; additive-only to prior modules. Reuse W12 child + W15 registry + W11 flow-port
+ W10 approval + W8 immutable-attempts + src/contracts. NO new dep/SDK/network (`deps {}`).
Deterministic (injected id/clock). Fail-closed escalation. D-02 (no flow.json write). Root
`/Users/Goodea/goodea/keryx` (branch feature/keryx-release2-extension-exec). Worktree-guard.
Commits/PR: NO co-authorship.

| ID | Kind | Model | Title / definition |
|----|------|-------|--------------------|
| T1 | context | Haiku | R2-1 scope + 3 scenarios + reuse surface + integration map (context.md). |
| T2 | implement | — | Umbrella: implement per plan (closed when T6 done). |
| T3 | test | — | Umbrella: TDD tests (closed when T5 authored + green). |
| T4 | review | — | Umbrella: review + completion (closed when T7 + completion done). |
| T5 | test (RED) | Sonnet | `src/harness/extension/execute.test.ts` (OFFLINE, injected id/clock + fake adapters). **(a) dispatchExtension (SC_R08_CHILD_DISPATCH_CANONICAL_RESULT):** a REGISTERED extension (registry `ok` + a `capabilityGrant`) + a coordinator `reservedBudget` → a canonical child dispatch that VALIDATES against the canonical `subagent-dispatch.schema.json` (+ the frozen `harness-child-contract-extension.schema.json` via `validateAgainstSchema`); a STATUS-first result string → `parseChildResult` yields a canonical `subagent-result` that VALIDATES against `subagent-result.schema.json` BEFORE persistence (assert the persisted form is the canonical object, not the raw STATUS string); the dispatch is bounded to the granted capabilities. **(b) evaluateExtensionGrant (SC_R08_EXTENSION_ESCALATION_REQUIRES_POLICY, NEGATIVE):** requested capabilities ⊆ granted → `{ok:true}`; a request for BROADER tools/provider access (⊄ granted) WITHOUT an explicit policy decision → DENY; WITH policy but WITHOUT provenance → DENY; WITH policy+provenance but WITHOUT a valid approval (W10 `checkApproval` invalid) → DENY; with policy AND provenance AND a valid approval → `{ok:true}`; each denial reason names the missing piece; out-of-enum capability/policy → fail-closed. Assert NO silent authority gain (a denied escalation grants nothing). **(c) retryWithContext (SC_R08_NEEDS_CONTEXT_ADAPTER):** a NEEDS_CONTEXT child result naming ONE missing bounded artifact → a retry dispatch with the SAME dispatch id, adding ONLY that artifact (assert the added-context set == {that artifact}); the prior attempt record is immutable (`expect(() => mutatePrior()).toThrow()` or deep-equality after retry). Deterministic (no Date.now/Math.random). RED before T6. |
| T6 | impl | Opus | GREEN. `src/harness/extension/execute.ts`: `dispatchExtension(input, deps)` (compose `buildChildDispatchExtension` + the registry-verified grant; bounded authority; `parseChildResult` for STATUS→canonical before persist), `evaluateExtensionGrant(input, deps)` (requested ⊆ granted ok; escalation requires policy+provenance+approval, else deny — reuse `inheritPolicy` containment idea + W10 `checkApproval` + `childProvenance`; fail-closed out-of-enum), `retryWithContext(input, deps)` (NEEDS_CONTEXT → same dispatch id + only the named artifact + prior attempt immutable, reuse W8). Additive helpers to `registry.ts`/`child/*` ONLY if strictly needed. Reuse `src/contracts` validators; validate dispatch/result against the frozen schemas. Injected id/clock; guard array indexing; `exactOptionalPropertyTypes` safe. NO new dep; extension writes no flow.json. Make T5 green. |
| T7 | review | Opus | code-verifier (`tsc` + full `bun test` ≥1210 + new green); **escalation fail-closed (KEY, adversarial):** is there ANY path to a broader tool/provider capability without an explicit policy decision AND provenance AND a valid approval? (try: out-of-enum capability, empty policy, stale/consumed approval, requested==granted edge, a capability not in the grant). If any grants silently → BLOCKED with the input. Canonical dispatch/result validate against the frozen schemas both directions; STATUS→canonical BEFORE persistence; NEEDS_CONTEXT retry = same dispatch id + only the named artifact + prior attempt immutable; **D-02** (`ctx rg writeFlow|flow.json src/harness/extension` = 0; extension/child write no flow.json; parent owns completion via ManagedFlowPort); reuse-only (W12/W15/W11/W10/W8 unmodified or additive-only — `git diff` confirms); determinism (no Date.now/Math.random); deps `{}` (no SDK/network); frozen requirements pkg + canonical schemas + src/eval + src/contracts + ADR-0001..0004 untouched; no regression. Lens: security + contract. |
