# Tasks — Flow 020 (Ollama provider + keryx harness CLI)

Task definitions live here; statuses live in flow.json (`keryx flow task done <id> <taskId>`).

Scope: new increment (beyond Release 1; NOT @release-2). New code under
`src/harness/provider/ollama/` + `src/commands/harness.ts`; additive `isLoopbackHost`
export + opt-in branch + CLI registration in `src/cli.ts`. Thin fetch/SSE, NO SDK →
`dependencies` STAYS `{}`. Reuse W5/W6/W7/W14/W15. Tests OFFLINE (recorded transcripts +
mocked fetch); live smoke is a manual orchestrator step. `allowLoopback` re-permits
LOOPBACK ONLY. Adapter/CLI never write flow.json (D-02). Deterministic tests. Worktree-guard.

| ID | Kind | Model | Title / definition |
|----|------|-------|--------------------|
| T1 | context | Haiku | Adapter/CLI map + egress opt-in design + reuse surface (context.md). |
| T2 | implement | — | Umbrella: implement per plan (closed when T6 done). |
| T3 | test | — | Umbrella: TDD tests (closed when T5 authored + impl green). |
| T4 | review | — | Umbrella: review + live smoke + completion (closed when T7/T8 + completion done). |
| T5 | test (RED) | Sonnet | OFFLINE RED tests. **(a) Ollama adapter** `src/harness/provider/ollama/ollama-provider.test.ts` — a recorded OpenAI-compat SSE transcript (role/message_start → content deltas → tool_calls delta → finish_reason → usage) replayed through `OllamaProvider.stream()` (injected `fetch`) yields the exact `NormalizedEventKind` sequence (model_start/text_delta.../tool_call_*/usage_update/model_end); `describe()`/descriptorDocument storage/retention/continuation=false; provider negatives (404 model-not-found→invalid_request, connection-refused/5xx→unavailable, malformed/torn→malformed, abort→cancelled) fail-closed; determinism. **(b) Egress opt-in** `src/harness/mutation/guard.loopback.test.ts` — `isLoopbackHost` TRUE for 127.0.0.1/127.1/`::1`/`[::1]`/localhost/encoded-loopback, FALSE for 169.254.169.254/10.x/192.168.x/8.8.8.8; and the adapter permits a loopback base URL ONLY with `allowLoopback:true` (without it → fail-closed), while a metadata/private-LAN base URL is DENIED even WITH `allowLoopback`. **(c) CLI** `src/commands/harness.test.ts` (or an offline harness-run test) — `keryx harness run --provider fake ...` assembles runOffline offline and prints a result; asserts structure (no live network). RED before T6. |
| T6 | impl | Opus (provider/security) | GREEN. `src/harness/provider/ollama/ollama-provider.ts` (`OllamaProvider implements ProviderPort`; thin fetch/SSE over `/v1/chat/completions`; reuse `sse.ts` parser) + `normalize.ts` (OpenAI-delta→NormalizedEvent, error taxonomy); additive `export function isLoopbackHost(host)` in `src/harness/mutation/guard.ts` (loopback-only subset; reuse existing decoder) + the adapter egress branch `permit iff !isPrivateEgressHost(host) || (grant.allowLoopback && isLoopbackHost(host))`; `src/commands/harness.ts` (`harnessCommand`: `run --provider fake|anthropic|ollama --model --base-url "<prompt>"` → wire runOffline with real deps + selected provider → print events/text/completion/evidence; anthropic reads ANTHROPIC_API_KEY, absent→fail-closed msg; ollama uses the loopback grant) + register in `src/cli.ts`. NO SDK; deps `{}`. Make T5 green. |
| T7 | review | Opus (security) | code-verifier (`tsc` + full `bun test` ≥1160 + new green, ALL OFFLINE); **egress opt-in narrow** (adversarial: `allowLoopback` reaches loopback ONLY — metadata 169.254 / private-LAN 10/172.16-31/192.168 / link-local DENIED even with the opt-in; a loopback host DENIED without the opt-in; the opt-in is per-grant, tied to the grant's own base URL; the Anthropic adapter's default egress is UNCHANGED and its tests still green); ProviderPort conformance (no SDK type across the port); provider negatives fail-closed; storage-off; **no live network in the suite** (no un-mocked fetch); CLI wiring correct + writes no flow.json; deps `{}` preserved; reuse-only (W5/W6/W7/W14 unmodified; W15/cli additive only); frozen requirements pkg + canonical schemas + src/eval + src/contracts + ADRs untouched. Lens: security + provider/contract. |
| T8 | verify (live smoke) | orchestrator | Run `bun ./src/cli.ts harness run --provider ollama --model llama3.1:latest "<prompt>"` against the local Ollama server; capture the end-to-end output (events → final text → completion/evidence). Confirm the adapter reaches localhost via the opt-in and normalizes a REAL model turn. Manual; NOT part of CI. Record the result in the flow journal. |
