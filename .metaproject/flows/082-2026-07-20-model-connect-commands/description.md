# Flow 082 — /model + /connect on the fly

Switch model (/model) or provider+key (/connect) mid-session: absolute-overlay picker → rebuild deps → persist → refresh labels → toast. History preserved. redetect() re-probes providers.
