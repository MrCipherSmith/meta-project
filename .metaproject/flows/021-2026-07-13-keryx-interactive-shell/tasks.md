# Tasks — Flow 021 (interactive keryx shell)

Task definitions live here; statuses via `keryx flow task done <id> <taskId>`.

Scope: bare `keryx` → interactive REPL over the reused W14/W20 providers. New
`src/commands/shell.ts`; additive edits to `src/cli.ts` (bare→shell, printHelp) +
`src/commands/harness.ts` (no-args→usage). NO SDK/new dep (`deps {}`). Reuse-only.
Tests OFFLINE (injected stdin/stdout + FakeProvider); live TTY = manual smoke. D-02.
Root = `/Users/Goodea/goodea/keryx` (branch feature/keryx-interactive-shell). Worktree-guard.

| ID | Kind | Model | Title / definition |
|----|------|-------|--------------------|
| T1 | context | Haiku | Shell design + reuse surface + testable-core contract (context.md). |
| T2 | implement | — | Umbrella: implement per plan (closed when T6 done). |
| T3 | test | — | Umbrella: TDD tests (closed when T5 authored + green). |
| T4 | review | — | Umbrella: review + live smoke + completion (closed when T7/T8 + completion done). |
| T5 | test (RED) | Sonnet | OFFLINE RED tests. **(a)** `src/commands/shell.test.ts` — drive `runShell(io, deps)` with `io.lines` = an async generator of input lines and `io.write` capturing output, a `FakeProvider` (recorded/inline transcripts keyed to the built request), and injected `clock`/`idSeq`. Assert: a user line streams the assistant `text_delta`s to `io.write` and `history` grows by exactly 2 (user+assistant); a second turn carries BOTH prior messages in the request (multi-turn); `/clear` resets `history` to empty; `/model llama3.2` and `/provider fake` switch the active model/provider for the next turn; `/help` prints help without a turn; `/exit` and end-of-input (EOF) terminate `runShell` cleanly (promise resolves); a turn whose provider yields a `provider_error` prints a readable error line and the loop CONTINUES (no throw). **(b)** `src/cli.test.ts` (or an offline dispatch test) — bare `main([])`/no-command dispatches to the shell (assert `shellCommand`/`runShell` invoked, injected so no real stdin); `--help` prints the command list INCLUDING `harness` + the shell. **(c)** `src/commands/harness.test.ts` addition — `keryx harness run` with empty `--provider` OR empty prompt prints the harness usage line and returns (no runOffline, no blocked JSON). RED before T6. |
| T6 | impl | Opus | GREEN. `src/commands/shell.ts`: `runShell(io, deps): Promise<void>` (the injectable REPL core — history, per-turn `NormalizedRequest` build, `provider.stream` consumption with live `text_delta` write, provider_error handling, slash commands `/help`,`/model`,`/provider`,`/clear`,`/exit`,`/quit`, EOF exit) + `shellCommand(args): Promise<void>` (thin wrapper: parse `--provider`/`--model`/`--base-url`; wire real stdin via `node:readline` async iterator + stdout write + a provider factory reusing harness.ts's selection incl. the Ollama loopback grant + real clock/uuid; default `ollama`/`llama3.1:latest`). Edit `src/cli.ts`: bare (no `args[0]`) → `shellCommand(args)`; keep `--help`/`-h`/`help` → `printHelp()`; update `printHelp` to include `harness run` + the interactive shell. Edit `src/commands/harness.ts`: if `provider` not in {fake,anthropic,ollama} OR `prompt` empty → print the harness usage line + return. Use `node:readline`/Bun stdin (stdlib, NO new dep). Guard array indexing. Make T5 green. |
| T7 | review | Opus | code-verifier (`tsc` + full `bun test` ≥ baseline + new green, ALL OFFLINE — no real TTY/network in tests); the REPL core is genuinely injectable (no `process.stdin`/`process.stdout` reached in `runShell`); multi-turn history correct; slash commands + EOF/`/exit` exit cleanly; provider_error non-fatal; bare `keryx`→shell + `--help`→command-list (incl. harness+shell) + `harness run` no-args→usage; reuse-only (W14/W20 providers + W15 egress + W5 types UNMODIFIED; only additive edits to cli.ts + harness.ts); `deps {}` (no readline/TTY dep added — stdlib only); no SDK; D-02 (no flow.json write in shell); frozen requirements pkg + canonical schemas + src/eval + src/contracts + ADRs untouched. Lens: logic + UX + security (confirm the shell adds no new egress; Ollama loopback grant + anthropic key-gate reused). |
| T8 | verify (live smoke) | orchestrator | Pipe input into bare `keryx` against the live Ollama: `printf 'hi in 3 words\n/exit\n' \| bun ./src/cli.ts` (and via the built `dist/cli.js`); confirm a real streamed assistant reply + clean exit. Also verify `keryx --help` lists harness+shell and `keryx harness run` (no args) prints usage. Manual; NOT CI. Record in the flow journal. |
