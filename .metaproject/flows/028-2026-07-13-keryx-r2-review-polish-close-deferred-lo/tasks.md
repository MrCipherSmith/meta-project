# Tasks — Flow 028 (R2 review polish — deferred LOW/INFO)

Statuses via `keryx flow task done 028 <taskId>`. TDD. Reuse-only; deterministic/offline; deps `{}`;
fail-closed preserved; D-02; frozen untouched. Root `/Users/Goodea/goodea/keryx` (branch
feature/keryx-r2-review-polish). Worktree-guard. Commits/PR: NO co-authorship. STOP-and-report if H
or E force a real prior-wave refactor.

| ID | Kind | Model | Title / definition |
|----|------|-------|--------------------|
| T1 | context | Haiku | Scope from the review deferred list (description.md). |
| T2 | implement | — | Umbrella: implement per plan (closed when T6 done). |
| T3 | test | — | Umbrella: TDD tests (closed when T5 authored + green). |
| T4 | review | — | Umbrella: review + completion (closed when T7 + completion done). |
| T5 | test (RED) | Sonnet | RED/lock tests (OFFLINE, injected id/clock + fakes) for the 8 items. **H** `isolation.test.ts`: parent WITH `maxToolCalls` + child OMITTING `maxToolCalls` → `inheritBudget` returns `{ok:false}` (deny, fail-closed, reason mentions tool-call); parent WITHOUT maxToolCalls + child omitting → still `{ok:true}` (unchanged); child WITH cap ≤ parent → ok (unchanged). If `planWaves` composes it: a cap-less child under a capped parent → wave denied. **A** `executor.test.ts`: pass run/session/correlation ids on the input → evidence `causal.runId`/`sessionId`/`correlationId` EQUAL those (not fresh idSeq). **B** a `makeProvider` factory test (anthropic with key; anthropic WITHOUT key → fake fallback; ollama; fake) returns the right provider type. **C** existing shell/harness/select tests stay green (DRY). **D** `provenance.test.ts`: omit `registrationProvenance` → record `trustLevel:"derived"` + taint-linked to the registry root. **E** `bound-wave.test.ts`: planning evidence `artifact.kind` is NOT `child-result:DONE` (neutral/PLANNED); per-attempt isolation still holds. **F** `executor.test.ts`: outcome `evidenceRefs` use the SAME prefixed encoding as the receipt. **G** `real-process-adapter.smoke.test.ts`: replace the tautological guard with an observable assertion; still inert (0 real spawns) without the flag. RED before T6. |
| T6 | impl | Opus | GREEN — implement the 8 items. **H** DENY (user decision): in `isolation.ts` `inheritBudget`, a child OMITTING `maxToolCalls` while the parent HAS a `maxToolCalls` budget → fail-closed deny (update the doc comment); parent-without-cap path unchanged; then `scheduler.ts` `decrementRemaining`/`planWaves` propagate the deny. Minimal tightening; STOP+report if it forces a real refactor or breaks an intended scenario. **A** add OPTIONAL `runId`/`sessionId`/`correlationId` to `RunContainedProcessInput`; `buildEvidence` uses them (fall back to idSeq when absent so existing callers don't break). **B** extract `makeProvider(name, model, baseUrl?, deps)` under `src/harness/provider/`; `shell.ts` + `harness.ts` call it (remove the duplicated switch). **C** `harness.ts` USAGE const; `shell.ts` `applySelection` helper; `select.ts` unify loop idiom. **D** covered by T5 (impl already supports the default — ensure the fallback path is exercised). **E** use a neutral/PLANNED disposition in `bound-wave.ts` `buildAttemptResult` (prefer an existing disposition value; do NOT extend a frozen enum without reporting). **F** normalize the executor outcome `evidenceRefs` to the prefixed form. **G** observable smoke guard. Reuse-only; injected id/clock; no real spawn offline; no flow.json; secrets not logged; deps `{}`. Make T5 green. |
| T7 | review | Opus | code-verifier (`tsc` + full `bun test` ≥1325/2skip + new green); **H** genuinely fail-closed (cap-less child under capped parent DENIED; no fail-open; runtime-dimension + parent-without-cap unchanged; W12/W13 no unintended regression — adversarial); **A** evidence causal ids = the real run/session/correlation (join works); **B/C** behavior-preserving (existing tests green; one provider factory, no drift); **E** planning evidence no longer reads as completion + per-attempt isolation intact + no frozen-enum change; **F** evidenceRefs consistent; **G** guard observable + still CI-inert (0 real spawns); reuse-only (prior modules unmodified except the minimal H/A/E touches — `git diff`); determinism (no Date.now/Math.random in offline cores); deps `{}`; D-02 (no flow.json write); secrets never logged; frozen requirements pkg + canonical schemas + src/eval + src/contracts + ADR untouched; no regression. Lens: security. |
