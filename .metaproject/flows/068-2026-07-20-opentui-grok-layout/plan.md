# Plan — flow 068
- T1 context: OpenTUI Box border/flex/padding API; grok layout from the screenshot. [done]
- T2 implement: header bar + token counter (io.onUsage), bordered composer, footer, user-message box; pure fmtTokens.
- T3 verify: tsc; bun test; fmtTokens unit; real-terminal look = user.
