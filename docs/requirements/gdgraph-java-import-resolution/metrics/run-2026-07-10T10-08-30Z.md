# Execution Metrics — docpack-orchestrator run

Version: 0.1.0

Opt-in metrics for the user-run docpack-orchestrator that produced the
`gdgraph-java-import-resolution` requirements package. Values the runtime does
not expose are marked `unknown`; approximations are marked `estimated` with a
source.

## Execution Metrics

| Metric | Value | Source |
|---|---|---|
| run_mode | user-direct (orchestrator invoked by user) | — |
| skill | docpack-orchestrator | — |
| model_used | claude-opus-4-8 | session |
| started_at | 2026-07-10T10:08:30Z | `date` at Phase 0 |
| finished_at | 2026-07-10T10:12:59Z | `date` at Phase 6 |
| wall_time_minutes | ~4.5 | estimated from timestamps |
| total_tool_calls | ~21 | estimated (counted from transcript, ±2) |
| shell_commands_run | 7 | counted (Bash calls this run) |
| files_read | 4 | draft doc, docpack SKILL.md, package-standard rule, roadmap |
| files_modified | 6 | 4 package docs created + roadmap edited + 1 loose doc deleted |
| lines_changed | +374 / -52 | `git diff --cached --stat docs/requirements` |
| tests_or_checks_run | 1 | Phase 4 structural verification (files/Version/links) |
| tests_or_checks_passed | 1 | verification passed |
| subagents_used | 0 | executed inline; this run did not dispatch subagents |
| subagent_names | none | — |
| tokens_total | unknown | runtime does not expose token counts to the agent |
| tokens_input | unknown | not exposed |
| tokens_output | unknown | not exposed |
| tokens_per_subagent | n/a | no subagents |
| context_sources_used | gdgraph code (build.ts/config.ts/seed.ts), existing draft, package-standard rule, roadmap, prior graph audit | — |
| keryx_used | partial | `keryx skills route` (ranked); `keryx gdskills route` attempt failed (wrong subcommand) |
| keryx_commands_run | 2 | `keryx gdskills route` (failed), `keryx skills route` (ok) |
| graph_used | yes | verified `back4/vantage-backend` graph artifacts + build.ts resolver code |
| wiki_used | no | not relevant to a requirements-package task |
| health_used | no | not relevant |
| artifacts_written | 5 | README, prd, specification, metrics-and-validation, this report; +1 roadmap row |
| blockers_or_retries | 2 | (1) `keryx gdskills route` unknown command → used skill directly; (2) roadmap Edit failed "file not read" → Read then Edit |
| final_status | success | package spec-ready, verification passed |

## Commands Run

- `keryx gdskills route docpack-orchestrator < …` — failed: unknown subcommand + input file already folded into package.
- `keryx skills route docpack-orchestrator` — ok: docpack-orchestrator ranked 150 (exact match). `route` ranks skills; it does not execute the orchestrator.
- `date -u` ×2 (start/finish stamps).
- `git add -A docs/requirements` + `git diff --cached --stat` (line counts).
- `grep`/`sed` reads of `src/gdgraph/build.ts` to verify the resolution-metric formula.

## Artifacts Changed

- Created: `docs/requirements/gdgraph-java-import-resolution/{README,prd,specification,metrics-and-validation}.md` and this metrics report.
- Modified: `docs/requirements/roadmap.md` (added one row).
- Deleted: `docs/requirements/gdgraph-java-import-resolution.md` (loose draft folded into the package per Iron Law 1).

## Comparison Notes

First run of this skill in the session — no prior baseline to compare against.
Evidence reused from the earlier gdgraph Java audit (same session) rather than
re-derived, which kept Phase 1 (Evidence) short.

## Metric Reliability

- Exact: wall time (timestamps), lines_changed (git), files_read/created (enumerated), keryx_commands_run, verification result.
- Estimated: total_tool_calls (±2, counted from transcript), wall_time rounded to 0.5 min.
- Unknown: all token counts — the runtime does not surface per-turn or
  per-subagent token usage to the agent, so these are honestly reported as
  `unknown` rather than guessed.
